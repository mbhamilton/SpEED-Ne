SpEED-Ne v2.4 has minor updates to fix a bug with where the Matlab function suptitle would generate an error. The app was also updated to remove text 
progress updates that could fill up the console window. The app now provides feedback via a progress bar. The numerical computations are unchanged. 
